/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://kalanden.vercel.app', // Your website URL
    generateRobotsTxt: true, // Generates robots.txt file
    sitemapSize: 5000, // Split sitemaps if there are more than 5000 URLs
  }
  